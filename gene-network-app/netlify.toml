[build]
  base = "gene-network-app/frontend"
  command = "npm run build"
  publish = "build"  # or "build" if you're using Create React App

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
